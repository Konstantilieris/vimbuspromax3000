# Project Manager Memory Index

This directory stores local operator memory and config for the repo-local PM/Jira agent pack in `.claude/agents`.

Committed scaffolding:

- `jira-config.template.md` - starter schema for local Jira configuration
- `team-defaults.template.md` - starter schema for local team and sprint defaults
- `slack-config.template.md` - starter schema for optional Slack notification settings

Expected local untracked files:

- `jira-config.md`
- `team-defaults.md`
- `slack-config.md`

Add live local memory files here only when you have real project-specific values.
